[pytest]
# The soak is minutes long, so it is not part of the ordinary run. Run it with
#   pytest -m slow
# or the whole suite including it with
#   pytest -m ""
addopts = -m "not slow"
markers =
    slow: long-running soak tests, deselected by default
